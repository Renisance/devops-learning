Тема планировщик cron. Задача каждый час записывать текущее дату и время в файл time.log
crontab -e (Заходим в редактор),
0 * * * * uptime -s >> time.log (Указываем дату, команда текущее время, добавить запись в конец файла time.log),
cat crontab -l (Проверяем задачу)